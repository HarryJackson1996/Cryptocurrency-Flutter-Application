import 'package:flutter/material.dart';

final lightTheme = ThemeData(brightness: Brightness.light, primaryColor: Colors.white, backgroundColor: Color.fromRGBO(0, 34, 69, 1));

final darkTheme = ThemeData(brightness: Brightness.dark, primaryColor: Colors.red, backgroundColor: Colors.black, buttonColor: Colors.red);
